# Tread Helper

A modern React + TypeScript + Vite application built as a helper tool.

## Tech Stack

- React 19
- TypeScript
- Vite
- Tailwind CSS
- Google GenAI SDK
- Recharts
- Lucide React
- Motion

## Getting Started

```bash
# Install dependencies (using Bun recommended)
bun install

# Or with npm
npm install

# Run development server
bun run dev
# or
npm run dev
```

The app will be available at `http://localhost:3000`.

## Available Scripts

| Command | Description |
|---------|-------------|
| `dev` | Start development server |
| `build` | Build for production |
| `preview` | Preview production build |
| `lint` | Type-check with TypeScript |
| `clean` | Remove build artifacts |

## Environment

Copy `.env.example` to `.env` and fill in the required values:

```bash
cp .env.example .env
```

## License

MIT
